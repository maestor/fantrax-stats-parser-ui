{
  // Copilot Chat relies on VS Code Terminal Shell Integration to reliably detect
  // commands and their outputs from the integrated terminal.
  "terminal.integrated.shellIntegration.enabled": true
}